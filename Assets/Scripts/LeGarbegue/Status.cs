using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Status : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField] int[] hp = new int[2];
    [SerializeField] int[] mp = new int[2];
    [SerializeField] int[] speed = new int[2];
    [SerializeField] int[] damage = new int[2];
    [SerializeField] int[] defense = new int[2];

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
